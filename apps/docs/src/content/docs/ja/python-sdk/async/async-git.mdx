---
title: "AsyncGit"
hideTitleOnPage: true
---

## AsyncGit \{#asyncgit\}

```python
class AsyncGit()
```

サンドボックス内での Git 操作をサポートします。

**例**:

```python
# リポジトリをクローンする
await sandbox.git.clone(
    url="https://github.com/user/repo.git",
    path="workspace/repo"
)

# リポジトリのステータスを確認する
status = await sandbox.git.status("workspace/repo")
print(f"変更されたファイル: {status.modified}")

# 変更をステージしてコミットする
await sandbox.git.add("workspace/repo", ["file.txt"])
await sandbox.git.commit(
    path="workspace/repo",
    message="ファイルを更新",
    author="John Doe",
    email="john@example.com"
)
```

#### AsyncGit.**init** \{#asyncgit__init__\}

```python
def __init__(api_client: GitApi)
```

新しい Git ハンドラのインスタンスを初期化します。

**引数**:

* `api_client` *GitApi* - サンドボックスの Git 操作用 API クライアント。

#### AsyncGit.add \{#asyncgitadd\}

```python
@intercept_errors(message_prefix="ファイルの追加に失敗しました: ")
@with_instrumentation()
async def add(path: str, files: list[str]) -> None
```

次回のコミットに向けて指定したファイルをステージします。コマンドラインで
&#39;git add&#39; を実行するのと同様です。

**引数**:

* `path` *str* - Gitリポジトリのルートへのパス。相対パスは
  サンドボックスの作業ディレクトリを基準に解決されます。
* `files` *list[str]* - リポジトリルートからの相対パスで指定する、ステージ対象のファイルまたはディレクトリのリスト。

**例**:

```python
# Stage a single file
await sandbox.git.add("workspace/repo", ["file.txt"])

# 複数ファイルをステージング
await sandbox.git.add("workspace/repo", [
    "src/main.py",
    "tests/test_main.py",
    "README.md"
])
```

#### AsyncGit.branches \{#asyncgitbranches\}

```python
@intercept_errors(message_prefix="ブランチの一覧取得に失敗しました: ")
@with_instrumentation()
async def branches(path: str) -> ListBranchResponse
```

リポジトリのブランチを一覧表示します。

**引数**:

* `path` *str* - Git リポジトリのルートパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。

**戻り値**:

* `ListBranchResponse` - リポジトリ内のブランチ一覧。

**例**:

```python
response = await sandbox.git.branches("workspace/repo")
print(f"ブランチ: {response.branches}")
```

#### AsyncGit.clone \{#asyncgitclone\}

```python
@intercept_errors(message_prefix="リポジトリのクローンに失敗しました: ")
@with_instrumentation()
async def clone(url: str,
                path: str,
                branch: str | None = None,
                commit_id: str | None = None,
                username: str | None = None,
                password: str | None = None) -> None
```

指定したパスに Git リポジトリをクローンします。特定のブランチまたはコミットのクローンに対応しており、認証情報が指定された場合はリモートリポジトリへの認証も行えます。

**引数**:

* `url` *str* - クローン元のリポジトリ URL。
* `path` *str* - リポジトリをクローンするパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。
* `branch` *str | None* - クローンするブランチ。未指定の場合はデフォルトブランチをクローンします。
* `commit_id` *str | None* - チェックアウトするコミット。指定した場合、リポジトリはそのコミットで detached HEAD 状態のままになります。
* `username` *str | None* - 認証に使用する Git のユーザー名。
* `password` *str | None* - 認証に使用する Git のパスワードまたはトークン。

**例**:

```python
# Clone the default branch
await sandbox.git.clone(
    url="https://github.com/user/repo.git",
    path="workspace/repo"
)

# 認証を使用して特定のブランチをクローンする
await sandbox.git.clone(
    url="https://github.com/user/private-repo.git",
    path="workspace/private",
    branch="develop",
    username="user",
    password="token"
)

# Clone a specific commit
await sandbox.git.clone(
    url="https://github.com/user/repo.git",
    path="workspace/repo-old",
    commit_id="abc123"
)
```

#### AsyncGit.commit \{#asyncgitcommit\}

```python
@intercept_errors(message_prefix="変更のコミットに失敗しました: ")
@with_instrumentation()
async def commit(path: str,
                 message: str,
                 author: str,
                 email: str,
                 allow_empty: bool = False) -> GitCommitResponse
```

ステージ済みの変更で新しいコミットを作成します。コミットする前に、必ず add() メソッドを使って変更をステージしてください。

**引数**:

* `path` *str* - Git リポジトリのルートへのパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。
* `message` *str* - 変更内容を記述するコミットメッセージ。
* `author` *str* - コミットの作成者名。
* `email` *str* - コミットの作成者のメールアドレス。
* `allow_empty` *bool* - 変更がステージされていない場合でも空のコミットの作成を許可します。デフォルトは False。

**例**:

```python
# Stage and commit changes
await sandbox.git.add("workspace/repo", ["README.md"])
await sandbox.git.commit(
    path="workspace/repo",
    message="Update documentation",
    author="John Doe",
    email="john@example.com",
    allow_empty=True
)
```

#### AsyncGit.push \{#asyncgitpush\}

```python
@intercept_errors(message_prefix="変更のプッシュに失敗しました: ")
@with_instrumentation()
async def push(path: str,
               username: str | None = None,
               password: str | None = None) -> None
```

現在のブランチのローカルコミットをすべてリモートリポジトリにプッシュします。リモートリポジトリで認証が必要な場合は、ユーザー名とパスワード／トークンを指定してください。

**引数**:

* `path` *str* - Gitリポジトリのルートへのパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。
* `username` *str | None* - 認証用のGitユーザー名。
* `password` *str | None* - 認証用のGitパスワードまたはトークン。

**例**:

```python
# 認証なしでプッシュ(パブリックリポジトリまたはSSH用)
await sandbox.git.push("workspace/repo")

# Push with authentication
await sandbox.git.push(
    path="workspace/repo",
    username="user",
    password="github_token"
)
```

#### AsyncGit.pull \{#asyncgitpull\}

```python
@intercept_errors(message_prefix="変更の取得に失敗しました: ")
@with_instrumentation()
async def pull(path: str,
               username: str | None = None,
               password: str | None = None) -> None
```

リモートリポジトリから変更を取得します。リモートリポジトリで認証が必要な場合は、
ユーザー名とパスワード／トークンを指定してください。

**引数**:

* `path` *str* - Git リポジトリのルートへのパス。相対パスは
  サンドボックスの作業ディレクトリを基準に解決されます。
* `username` *str | None* - 認証用の Git ユーザー名。
* `password` *str | None* - 認証用の Git パスワードまたはトークン。

**例**:

```python
# 認証なしでプル
await sandbox.git.pull("workspace/repo")

# Pull with authentication
await sandbox.git.pull(
    path="workspace/repo",
    username="user",
    password="github_token"
)
```

#### AsyncGit.status \{#asyncgitstatus\}

```python
@intercept_errors(message_prefix="ステータスの取得に失敗しました: ")
@with_instrumentation()
async def status(path: str) -> GitStatus
```

現在の Git リポジトリの状態を取得します。

**引数**:

* `path` *str* - Git リポジトリのルートへのパス。相対パスは
  サンドボックスの作業ディレクトリを基準に解決されます。

**戻り値**:

* `GitStatus` - リポジトリの状態情報。以下を含みます:
  * current&#95;branch: 現在のブランチ名
  * file&#95;status: ファイルの状態一覧
  * ahead: リモートへ未プッシュのローカルコミット数
  * behind: ローカルに未取得のリモートコミット数
  * branch&#95;published: ブランチがリモートリポジトリに公開済みかどうか

**例**:

```python
status = await sandbox.git.status("workspace/repo")
print(f"ブランチ: {status.current_branch}")
print(f"Commits ahead: {status.ahead}")
print(f"Commits behind: {status.behind}")
```

#### AsyncGit.checkout_branch \{#asyncgitcheckout_branch\}

```python
@intercept_errors(message_prefix="ブランチのチェックアウトに失敗しました: ")
@with_instrumentation()
async def checkout_branch(path: str, branch: str) -> None
```

リポジトリのブランチをチェックアウトします。

**引数**:

* `path` *str* - Gitリポジトリのルートへのパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。
* `branch` *str* - チェックアウトするブランチ名

**例**:

```python
# ブランチをチェックアウト
await sandbox.git.checkout_branch("workspace/repo", "feature-branch")
```

#### AsyncGit.create_branch \{#asyncgitcreate_branch\}

```python
@intercept_errors(message_prefix="ブランチの作成に失敗しました: ")
@with_instrumentation()
async def create_branch(path: str, name: str) -> None
```

リポジトリ内にブランチを作成します。

**引数**:

* `path` *str* - Git リポジトリのルートへのパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。
* `name` *str* - 作成する新しいブランチ名

**例**:

```python
# 新しいブランチを作成
await sandbox.git.create_branch("workspace/repo", "new-feature")
```

#### AsyncGit.delete_branch \{#asyncgitdelete_branch\}

```python
@intercept_errors(message_prefix="ブランチの削除に失敗しました: ")
@with_instrumentation()
async def delete_branch(path: str, name: str) -> None
```

リポジトリ内のブランチを削除します。

**引数**:

* `path` *str* - Git リポジトリのルートへのパス。相対パスはサンドボックスの作業ディレクトリを基準に解決されます。
* `name` *str* - 削除するブランチ名

**例**:

```python
# ブランチを削除
await sandbox.git.delete_branch("workspace/repo", "old-feature")
```

## GitCommitResponse \{#gitcommitresponse\}

```python
class GitCommitResponse()
```

Git のコミットからのレスポンス。

**属性**:

* `sha` *str* - コミットのSHA
