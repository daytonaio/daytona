---
title: "チャート"
hideTitleOnPage: true
---

## グラフ \{#chart\}

```python
class Chart(BaseModel)
```

matplotlib のメタデータを含むチャートを表します。

**属性**:

* `type` *ChartType* - チャートのタイプ
* `title` *str* - チャートのタイトル
* `elements` *list[Any]* - チャートの要素
* `png` *str | None* - base64 エンコードされたチャートの PNG 表現

#### Chart.to_dict \{#chartto_dict\}

```python
def to_dict() -> dict[str, Any]
```

チャートの作成に使用されたメタデータの辞書を返します。

## ChartType \{#charttype\}

```python
class ChartType(str, Enum)
```

チャートの種類

**列挙メンバー**:

* `LINE` (&quot;line&quot;)
  * `SCATTER` (&quot;scatter&quot;)
  * `BAR` (&quot;bar&quot;)
  * `PIE` (&quot;pie&quot;)
  * `BOX_AND_WHISKER` (&quot;box&#95;and&#95;whisker&quot;)
  * `COMPOSITE_CHART` (&quot;composite&#95;chart&quot;)
  * `UNKNOWN` (&quot;unknown&quot;)

## Chart2D \{#chart2d\}

```python
class Chart2D(Chart)
```

メタデータを含む2Dチャートを表します。

**属性**:

* `x_label` *str | None* - x軸のラベル
* `y_label` *str | None* - y軸のラベル

## PointData \{#pointdata\}

```python
class PointData(BaseModel)
```

2D チャート上の点を表します。

**属性**:

* `label` *str* - 点のラベル
* `points` *list[tuple[str | float, str | float]]* - チャート上の座標点のリスト

## PointChart \{#pointchart\}

```python
class PointChart(Chart2D)
```

メタデータを含む散布図を表します。

**属性**:

* `x_ticks` *list[str | float]* - x軸の目盛
* `x_tick_labels` *list[str]* - x軸の目盛ラベル
* `x_scale` *str* - x軸のスケール
* `y_ticks` *list[str | float]* - y軸の目盛
* `y_tick_labels` *list[str]* - y軸の目盛ラベル
* `y_scale` *str* - y軸のスケール
* `elements` *list[PointData]* - 図のプロット点

## 折れ線グラフ \{#linechart\}

```python
class LineChart(PointChart)
```

メタデータ付きの折れ線グラフを表します。

**属性**:

* `type` *ChartType* - グラフのタイプ

## 散布図 \{#scatterchart\}

```python
class ScatterChart(PointChart)
```

メタデータ付きの散布図を表します。

**属性**:

* `type` *ChartType* - チャートのタイプ

## BarData \{#bardata\}

```python
class BarData(BaseModel)
```

棒グラフのバーを表します。

**属性**:

* `label` *str* - バーのラベル
* `group` *str* - バーのグループ
* `value` *str* - バーの値

## 棒グラフ（BarChart） \{#barchart\}

```python
class BarChart(Chart2D)
```

メタデータを含む棒グラフを表します。

**属性**:

* `type` *ChartType* - チャートの種類
* `elements` *list[BarData]* - 棒のデータ要素

## PieData \{#piedata\}

```python
class PieData(BaseModel)
```

円グラフのスライス（扇形）を表します。

**属性**:

* `label` *str* - スライスのラベル
* `angle` *float* - スライスの角度
* `radius` *float* - スライスの半径
* `autopct` *str | float* - スライスの autopct 値

## 円グラフ \{#piechart\}

```python
class PieChart(Chart)
```

メタデータを含む円グラフを表します。

**属性**:

* `type` *ChartType* - グラフの種類
* `elements` *list[PieData]* - グラフのスライス（扇形）

## BoxAndWhiskerData（箱ひげ図データ） \{#boxandwhiskerdata\}

```python
class BoxAndWhiskerData(BaseModel)
```

箱ひげ図における各ボックス（箱）とひげを表します。

**属性**:

* `label` *str* - ボックスのラベル
* `min` *float* - 最小値
* `first_quartile` *float* - 第1四分位数
* `median` *float* - 中央値
* `third_quartile` *float* - 第3四分位数
* `max` *float* - 最大値
* `outliers` *list[float]* - 外れ値

## 箱ひげ図 \{#boxandwhiskerchart\}

```python
class BoxAndWhiskerChart(Chart2D)
```

メタデータを含む箱ひげ図を表します。

**属性**:

* `type` *ChartType* - グラフの種類
* `elements` *list[BoxAndWhiskerData]* - グラフの箱ひげデータ

## CompositeChart \{#compositechart\}

```python
class CompositeChart(Chart)
```

メタデータを伴う複合チャートを表します。複合チャートは、複数のチャート（サブプロット）から構成されるチャートです。

**属性**:

* `type` *ChartType* - チャートのタイプ
* `elements` *list[Chart]* - 複合チャートを構成するチャート（サブプロット）
