---
title: "エラー"
hideTitleOnPage: true
---

## DaytonaError \{#daytonaerror\}

Daytona SDK の基底エラークラスです。

**継承元:**

* `Error`

### このクラスを拡張しているクラス \{#extended-by\}

* `DaytonaNotFoundError`
* `DaytonaRateLimitError`

### コンストラクタ \{#constructors\}

#### new DaytonaError() \{#new-daytonaerror\}

*継承元*: `Error.constructor`

```ts
new DaytonaError(message?: string): DaytonaError
```

**パラメータ**:

* `message?` *string*

**戻り値**:

* `DaytonaError`

#### new DaytonaError() \{#new-daytonaerror\}

*継承元*: `Error.constructor`

```ts
new DaytonaError(message?: string, options?: ErrorOptions): DaytonaError
```

**パラメーター**:

* `message?` *string*
* `options?` *ErrorOptions*

**戻り値**:

* `DaytonaError`

***

## DaytonaNotFoundError \{#daytonanotfounderror\}

Daytona SDK における基本となるエラークラスです。

**継承元:**

* `DaytonaError`

### コンストラクタ \{#constructors\}

#### new DaytonaNotFoundError() \{#new-daytonanotfounderror\}

*継承元*: `DaytonaError.constructor`

```ts
new DaytonaNotFoundError(message?: string): DaytonaNotFoundError
```

**パラメーター**:

* `message?` *string*

**戻り値**:

* `DaytonaNotFoundError`

#### new DaytonaNotFoundError() \{#new-daytonanotfounderror\}

*継承元*: `DaytonaError.constructor`

```ts
new DaytonaNotFoundError(message?: string, options?: ErrorOptions): DaytonaNotFoundError
```

**パラメータ**:

* `message?` *string*
* `options?` *ErrorOptions*

**戻り値**:

* `DaytonaNotFoundError`

***

## DaytonaRateLimitError \{#daytonaratelimiterror\}

レート制限を超過した場合に送出されるエラーです。

**継承元:**

* `DaytonaError`

### コンストラクタ \{#constructors\}

#### new DaytonaRateLimitError() \{#new-daytonaratelimiterror\}

*継承元*: `DaytonaError.constructor`

```ts
new DaytonaRateLimitError(message?: string): DaytonaRateLimitError
```

**パラメーター**:

* `message?` *string*

**戻り値**:

* `DaytonaRateLimitError`

#### new DaytonaRateLimitError() \{#new-daytonaratelimiterror\}

*継承元*: `DaytonaError.constructor`

```ts
new DaytonaRateLimitError(message?: string, options?: ErrorOptions): DaytonaRateLimitError
```

**パラメータ**:

* `message?` *string*
* `options?` *ErrorOptions*

**戻り値**:

* `DaytonaRateLimitError`
