---
title: サンドボックス管理
---

import Label from '@components/Label.astro'
import { Tabs, TabItem } from '@astrojs/starlight/components';

<Label>
  Distribution: **Open Source**
</Label>

サンドボックスはDaytonaによって管理される分離された開発環境です。このガイドでは、SDKを使用してサンドボックスを作成、管理、削除する方法について説明します。

## Sandboxの作成

Daytona SDKは、デフォルトまたはカスタム設定でSandboxを作成するオプションを提供します。Sandboxの言語、イメージ、リソース、環境変数、ボリュームを指定できます。

### 基本的なSandboxの作成

Daytona SDKは、PythonとTypeScriptを使用して、デフォルト設定、特定の言語、またはカスタムIDでSandboxを作成するメソッドを提供します。

<Tabs>
<TabItem label="Python" icon="seti:python">
```python
from daytona_sdk import Daytona

daytona = Daytona()

# Create a basic Sandbox

workspace = daytona.create()

# Create a Sandbox with specific language

params = CreateWorkspaceParams(language="python")
workspace = daytona.create(params)

# Create a Sandbox with custom ID

params = CreateWorkspaceParams(id="my-workspace")
workspace = daytona.create(params)

```
</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">
```typescript
import { Daytona } from '@daytonaio/sdk';

const daytona = new Daytona();

// Create a basic workspace
const workspace = await daytona.create();

// Create a workspace with specific language
const workspace = await daytona.create({ language: 'typescript' });

// Create a workspace with custom ID
const workspace = await daytona.create({ id: 'my-workspace' });
```

</TabItem>
</Tabs>

## Sandbox情報

Daytona SDKは、PythonとTypeScriptを使用してSandboxのID、ルートディレクトリ、ステータスなどの情報を取得するメソッドを提供します。

<Tabs>
<TabItem label="Python" icon="seti:python">
```python
# Get Sandbox ID
workspace_id = workspace.id

# Get Sandbox root directory

root_dir = workspace.get_workspace_root_dir()

```
</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">
```typescript
// Get workspace ID
const workspaceId = workspace.id;

// Get workspace root directory
const rootDir = await workspace.getWorkspaceRootDir();
```

</TabItem>
</Tabs>

## Sandboxを削除

Daytona SDKは、PythonとTypeScriptを使用してSandboxを削除するなど、Sandbox上で操作を実行するためのメソッドを提供します。

<Tabs>
<TabItem label="Python" icon="seti:python">
```python
# Remove Sandbox
daytona.remove(workspace)
```
</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">
```typescript
// Remove workspace
await daytona.remove(workspace);
```
</TabItem>
</Tabs>
