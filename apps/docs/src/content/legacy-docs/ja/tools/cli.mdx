---
title: CLI
description: Daytona CLIを使用してサポートされている操作のリファレンス。
sidebar:
  label: Daytona CLIリファレンス
---
import Aside from "@components/Aside.astro";
import Label from "@components/Label.astro";

<Label>
  Distribution: **Open Source**, **Cloud**, **Self-Managed**
</Label>

`daytona`コマンドラインツールは、Daytonaのコア機能へのアクセスを提供します。
`daytona`ツールは以下の操作に使用できます：

* Daytona Serverのライフサイクル管理。
* [Workspaces](../usage/workspaces)、[Git Providers](../configuration/git-providers)、[Providers](../configuration/providers)、およびその他のDaytonaコンポーネントの管理。
* [Daytona Server](../configuration/server)のインタラクティブな設定。

このリファレンスでは、`daytona`コマンドラインツールでサポートされているすべてのコマンドを、その動作の説明とサポートされているフラグと共に一覧表示しています。
`daytona`を実行する際に`--help`/`-h`フラグを追加することで、コマンドごとにこのドキュメントにアクセスできます。

<Aside type="note">
このリファレンスは、Workspace内で実行される`daytona`コマンドには適用されません。
</Aside>

## daytona

Daytonaは開発環境マネージャーです

```shell
daytona [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |
| `--version` | `-v` | Daytonaのバージョンを表示 |

## daytona api-key

APIキーコマンド

```shell
daytona api-key [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona api-key create

新しいAPIキーを作成する

```shell
daytona api-key create [NAME] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona api-key delete

APIキーを削除する

```shell
daytona api-key delete [NAME] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--yes` | `-y` | 確認プロンプトをスキップ |
| `--help` |  | daytonaのヘルプ |

## daytona api-key list

APIキーを一覧表示

```shell
daytona api-key list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。(yaml, json)のいずれかである必要があります |
| `--help` |  | daytonaのヘルプ |

## daytona autocomplete

シェル環境用の補完スクリプトを追加します

```shell
daytona autocomplete [bash|zsh|fish|powershell] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

<Aside type="note">
bashシェル環境を使用している場合は、完全な自動補完機能を利用するためにbash-completionがインストールされていることを確認してください。
Linuxでのインストール: ```sudo apt-get install bash-completion```
macOSでのインストール: ```brew install bash-completion```
</Aside>

## daytona build

ビルドを管理する

```shell
daytona build [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona build delete

ビルドを削除する

```shell
daytona build delete [BUILD] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのビルドを削除 |
| `--force` | `-f` | ビルドを強制削除 |
| `--prebuild-id` |  | プリビルドからすべてのビルドを削除 |
| `--help` |  | daytonaのヘルプ |

## daytona build info

ビルド情報を表示

```shell
daytona build info [BUILD] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona build list

すべてのビルドを一覧表示

```shell
daytona build list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytonaのヘルプ |

## daytona build logs

ビルドのログを表示

```shell
daytona build logs [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--follow` | `-f` | ログをフォロー |
| `--help` |  | daytonaのヘルプ |

## daytona build run

ワークスペーステンプレートからビルドを実行する

```shell
daytona build run [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytona のヘルプ |

## daytona code

お好みのIDEでワークスペースを開く

```shell
daytona code [WORKSPACE] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--ide` | `-i` | IDEを指定する (vscode, code-insiders, browser, cursor, codium, codium-insiders, ssh, jupyter, fleet, positron, zed, windsurf, clion, goland, intellij, phpstorm, pycharm, rider, rubymine, webstorm) |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytonaのヘルプ |

## daytona config

Daytonaの設定を出力

```shell
daytona config [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--show-api-keys` | `-k` | APIキーを表示 |
| `--help` |  | daytonaのヘルプ |

## daytona create

ワークスペースを作成する

```shell
daytona create [REPOSITORY_URL | WORKSPACE_CONFIG_NAME]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--blank` |  | 既存のテンプレートを使用せずに空のワークスペースを作成する |
| `--branch` |  | ワークスペースで使用するGitブランチを指定する |
| `--builder` |  | ビルダーを指定する（現在はauto/devcontainer/none） |
| `--custom-image` |  | フラグ値として渡されたカスタムイメージでワークスペースを作成する；--custom-image-userフラグも設定する必要があります |
| `--custom-image-user` |  | フラグ値として渡されたカスタムイメージユーザーでワークスペースを作成する；--custom-imageフラグも設定する必要があります |
| `--devcontainer-path` |  | フラグ値として渡されたパスでdevcontainerビルダーを自動的に割り当てる |
| `--env` |  | 環境変数を指定する（例：--env 'KEY1=VALUE1' --env 'KEY2=VALUE2' ...） |
| `--git-provider-config` |  | GitプロバイダーのコンフィグIDまたはエイリアスを指定する |
| `--ide` | `-i` | IDEを指定する（vscode, code-insiders, browser, cursor, codium, codium-insiders, ssh, jupyter, fleet, positron, zed, windsurf, clion, goland, intellij, phpstorm, pycharm, rider, rubymine, webstorm） |
| `--label` |  | ラベルを指定する（例：--label 'label.key1=VALUE1' --label 'label.key2=VALUE2' ...） |
| `--manual` |  | Gitリポジトリを手動で入力する |
| `--multi-workspace` |  | 複数のワークスペース/リポジトリでターゲットを指定する |
| `--no-ide` | `-n` | ターゲット作成後にIDEでターゲットを開かない |
| `--target` | `-t` | ターゲットを指定する（例：'local'） |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytonaのヘルプ |

## daytona delete

ワークスペースを削除する

```shell
daytona delete [WORKSPACE]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのワークスペースを削除 |
| `--force` | `-f` | ワークスペースを強制削除 |
| `--yes` | `-y` | プロンプトなしで削除を確認 |
| `--help` |  | daytona のヘルプ |

## daytona docs

デフォルトブラウザでDaytonaドキュメントを開きます。

```shell
daytona docs [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona env

すべてのターゲットとワークスペースに追加されるサーバー環境変数を管理する

```shell
daytona env [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona env delete

サーバー環境変数を削除

```shell
daytona env delete [KEY]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona env list

サーバー環境変数を一覧表示

```shell
daytona env list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--show-values` | `-v` | 環境変数の値を表示 |
| `--help` |  | daytona のヘルプ |

## daytona env set

サーバー環境変数を設定する

```shell
daytona env set [KEY=VALUE]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona forward

ワークスペースからローカルマシンにポートを転送します

```shell
daytona forward [PORT] [WORKSPACE] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--public` |  | ポートをURL経由で公開利用可能にするかどうか |
| `--help` |  | daytonaのヘルプ |

## daytona git-provider

Git プロバイダー設定を管理

```shell
daytona git-provider [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytona のヘルプ |

## daytona git-provider create

Gitプロバイダー設定を作成する

```shell
daytona git-provider create [GIT_PROVIDER_ID] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--alias` | `-a` | エイリアス |
| `--base-api-url` | `-b` | ベースAPI URL |
| `--signing-key` | `-k` | 署名キー |
| `--signing-method` | `-s` | 署名方法 (ssh, gpg) |
| `--token` | `-t` | 個人アクセストークン |
| `--username` | `-u` | ユーザー名 |
| `--help` |  | daytonaのヘルプ |

## daytona git-provider delete

Git プロバイダー設定を削除する

```shell
daytona git-provider delete [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべての Git プロバイダーを削除 |
| `--yes` | `-y` | プロンプトなしで削除を確認 |
| `--help` |  | daytona のヘルプ |

## daytona git-provider list

登録されたGitプロバイダー設定を一覧表示します

```shell
daytona git-provider list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytonaのヘルプ |

## daytona git-provider update

Gitプロバイダーを更新する

```shell
daytona git-provider update [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona ide

デフォルトのIDEを選択

```shell
daytona ide [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona info

ワークスペース情報を表示

```shell
daytona info [WORKSPACE] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona list

ワークスペースを一覧表示

```shell
daytona list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--label` | `-l` | ラベルでフィルタリング |
| `--help` |  | daytona のヘルプ |

## daytona logs

ワークスペースのログを表示する

```shell
daytona logs [WORKSPACE] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--follow` | `-f` | ログをフォローする |
| `--help` |  | daytona のヘルプ |

## daytona prebuild

プリビルドを管理する

```shell
daytona prebuild [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona prebuild create

プリビルド設定を作成する

```shell
daytona prebuild create [WORKSPACE_CONFIG] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--branch` | `-b` | プリビルド用のGitブランチ |
| `--commit-interval` | `-c` | プリビルドを実行するコミット間隔 - プッシュイベントを無視する場合は空白のままにする |
| `--retention` | `-r` | 同時に保存される結果ビルドの最大数 |
| `--run` |  | 追加後にプリビルドを一度実行する |
| `--trigger-files` | `-t` | 変更時に明示的にプリビルドをトリガーするファイルのフルパス |
| `--help` |  | daytonaのヘルプ |

## daytona prebuild delete

プリビルド設定を削除する

```shell
daytona prebuild delete [WORKSPACE_CONFIG] [PREBUILD] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--force` | `-f` | プリビルドを強制削除する |
| `--help` |  | daytonaのヘルプ |

## daytona prebuild info

プリビルド設定情報を表示

```shell
daytona prebuild info [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona prebuild list

プリビルド設定を一覧表示

```shell
daytona prebuild list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona prebuild update

プリビルド設定を更新する

```shell
daytona prebuild update [WORKSPACE_CONFIG] [PREBUILD_ID] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--branch` | `-b` | プリビルド用のGitブランチ |
| `--commit-interval` | `-c` | プリビルドを実行するコミット間隔 - プッシュイベントを無視する場合は空白のままにする |
| `--retention` | `-r` | 同時に保存される結果ビルドの最大数 |
| `--run` |  | 更新後にプリビルドを一度実行する |
| `--trigger-files` | `-t` | 変更によってプリビルドを明示的にトリガーするファイルのフルパス |
| `--help` |  | daytonaのヘルプ |

## daytona profile

プロファイルを管理

```shell
daytona profile [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona profile create

プロファイルを作成する

```shell
daytona profile create [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--api-key` | `-k` | APIキー |
| `--api-url` | `-a` | API URL |
| `--name` | `-n` | プロファイル名 |
| `--help` |  | daytonaのヘルプ |

## daytona profile delete

プロファイルを削除する

```shell
daytona profile delete [PROFILE_NAME] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona profile list

プロファイルを一覧表示

```shell
daytona profile list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona profile update

プロファイル [PROFILE_NAME] を更新

```shell
daytona profile update [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--api-key` | `-k` | API Key |
| `--api-url` | `-a` | API URL |
| `--name` | `-n` | プロファイル名 |
| `--help` |  | daytona のヘルプ |

## daytona provider

プロバイダーを管理

```shell
daytona provider [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona provider install

プロバイダーをインストールする

```shell
daytona provider install [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytonaのヘルプ |

## daytona provider list

インストールされたプロバイダーを一覧表示

```shell
daytona provider list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona provider uninstall

プロバイダーをアンインストール

```shell
daytona provider uninstall [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona provider update

プロバイダーを更新

```shell
daytona provider update [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのプロバイダーを更新 |
| `--help` |  | daytona のヘルプ |

## daytona purge

現在のデバイスからすべてのDaytonaデータを削除します

```shell
daytona purge [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--force` | `-f` | すべてのターゲットを強制的に削除 |
| `--yes` | `-y` | プロンプトなしで削除を実行 |
| `--help` |  | daytonaのヘルプ |

## daytona restart

ワークスペースを再起動する

```shell
daytona restart [WORKSPACE]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona runner

ランナーを管理する

```shell
daytona runner [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona runner config

Daytona Runnerの設定を出力します

```shell
daytona runner config [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--key` | `-k` | APIキーを表示 |
| `--help` |  | daytonaのヘルプ |

## daytona runner configure

Daytona Runnerを設定する

```shell
daytona runner configure [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--api-key` |  | Runner APIキー |
| `--api-url` |  | Daytona Server API URL |
| `--client-id` |  | クライアントID |
| `--disable-telemetry` |  | テレメトリを無効にする |
| `--id` |  | Runner ID |
| `--name` |  | Runner名 |
| `--help` |  | daytonaのヘルプ |

## daytona runner logs

ランナーログを表示

```shell
daytona runner logs [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--follow` | `-f` | ログをフォロー |
| `--help` |  | daytonaのヘルプ |

## daytona runner purge

Daytona Runnerをパージします

```shell
daytona runner purge [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--yes` | `-y` | プロンプトなしでDaytona Runnerパージを実行 |
| `--help` |  | daytonaのヘルプ |

## daytona runner restart

ランナーを再起動します

```shell
daytona runner restart [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona runner serve

ランナーをフォアグラウンドで開始します

```shell
daytona runner serve [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona runner start

ランナーを開始します

```shell
daytona runner start [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona runner stop

ランナーを停止します

```shell
daytona runner stop [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona serve

現在のターミナルセッションでサーバープロセスを実行します

```shell
daytona serve [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona server

サーバープロセスをデーモンモードで開始します

```shell
daytona server [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--yes` | `-y` | 確認プロンプトをスキップします |
| `--help` |  | daytona のヘルプ |

## daytona server config

ローカルDaytona Serverの設定を出力

```shell
daytona server config [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytonaのヘルプ |

## daytona server configure

Daytona Serverを設定する

```shell
daytona server configure [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona server logs

Daytona Serverのログを出力する

```shell
daytona server logs [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--file` |  | 特定のログファイルを読み込む |
| `--follow` | `-f` | ログを追跡する |
| `--local` | `-l` | ローカルサーバーのログファイルを読み込む |
| `--help` |  | daytonaのヘルプ |

## daytona server logs list

Daytona Server ログファイルを一覧表示します

```shell
daytona server logs list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytona のヘルプ |

## daytona server restart

Daytona Serverデーモンを再起動します

```shell
daytona server restart [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona server runner

ランナーを管理する

```shell
daytona server runner [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona server runner create

ランナーを作成する

```shell
daytona server runner create [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--name` | `-n` | ランナー名 |
| `--help` |  | daytona のヘルプ |

## daytona server runner delete

ランナーを削除する

```shell
daytona server runner delete [RUNNER] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--yes` | `-y` | プロンプトなしで削除を確認する |
| `--help` |  | daytonaのヘルプ |

## daytona server runner list

ランナーを一覧表示

```shell
daytona server runner list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona server runner logs

ランナーログを表示

```shell
daytona server runner logs [RUNNER_ID] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--follow` | `-f` | ログをフォロー |
| `--help` |  | daytonaのヘルプ |

## daytona server start

Daytona Serverデーモンを開始する

```shell
daytona server start [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona server stop

Daytona Serverデーモンを停止します

```shell
daytona server stop [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona ssh

ターミナルを使用してワークスペースにSSH接続する

```shell
daytona ssh [WORKSPACE] [CMD...] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--edit` | `-e` | ワークスペースのSSH設定を編集する |
| `--option` | `-o` | KEY=VALUE形式でSSHオプションを指定する。 |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytonaのヘルプ |

## daytona start

ワークスペースを開始する

```shell
daytona start [WORKSPACE]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのターゲットを開始する |
| `--code` | `-c` | ターゲット開始後にIDEでターゲットを開く |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytonaのヘルプ |

## daytona stop

ワークスペースを停止する

```shell
daytona stop [WORKSPACE]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのターゲットを停止する |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytona のヘルプ |

## daytona target-config

ターゲット設定を管理する

```shell
daytona target-config [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona target-config create

ターゲット設定を作成

```shell
daytona target-config create [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--file` | `-f` | ターゲット設定用のJSONファイルのパス、標準入力から読み取る場合は'-'を使用 |
| `--help` |  | daytonaのヘルプ |

## daytona target-config delete

ターゲット設定を削除します

```shell
daytona target-config delete [TARGET_CONFIG] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--yes` | `-y` | プロンプトなしですべてのターゲットの削除を確認 |
| `--help` |  | daytonaのヘルプ |

## daytona target-config list

ターゲット設定を一覧表示

```shell
daytona target-config list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--show-options` | `-v` | ターゲットオプションを表示 |
| `--help` |  | daytona のヘルプ |

## daytona target

ターゲットを管理する

```shell
daytona target [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona target create

ターゲットを作成する

```shell
daytona target create [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona target delete

ターゲットを削除する

```shell
daytona target delete [TARGET]... [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのターゲットを削除 |
| `--force` | `-f` | ターゲットを強制削除 |
| `--yes` | `-y` | プロンプトなしで削除を確認 |
| `--help` |  | daytonaのヘルプ |

## daytona target info

ターゲット情報を表示

```shell
daytona target info [TARGET] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。(yaml, json) のいずれかである必要があります |
| `--show-options` | `-v` | ターゲットオプションを表示 |
| `--help` |  | daytona のヘルプ |

## daytona target list

ターゲットを一覧表示

```shell
daytona target list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。(yaml, json) のいずれかである必要があります |
| `--show-options` | `-v` | ターゲットオプションを表示 |
| `--help` |  | daytona のヘルプ |

## daytona target logs

ターゲットのログを表示する

```shell
daytona target logs [TARGET] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--follow` | `-f` | ログをフォローする |
| `--help` |  | daytonaのヘルプ |

## daytona target restart

ターゲットを再起動する

```shell
daytona target restart [TARGET] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona target set-default

デフォルトターゲットを設定

```shell
daytona target set-default [TARGET] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytona のヘルプ |

## daytona target ssh

ターミナルを使用してターゲットにSSH接続する

```shell
daytona target ssh [TARGET] [CMD...] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--option` | `-o` | KEY=VALUE形式でSSHオプションを指定します。 |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認します |
| `--help` |  | daytonaのヘルプ |

## daytona target start

ターゲットを開始する

```shell
daytona target start [TARGET] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのターゲットを開始する |
| `--yes` | `-y` | すべてのプロンプトを自動的に確認する |
| `--help` |  | daytonaのヘルプ |

## daytona target stop

ターゲットを停止する

```shell
daytona target stop [TARGET] [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのターゲットを停止する |
| `--help` |  | daytona のヘルプ |

## daytona telemetry

テレメトリ収集を管理する

```shell
daytona telemetry [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona telemetry disable

テレメトリ収集を無効にする

```shell
daytona telemetry disable [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona telemetry enable

テレメトリ収集を有効にする

```shell
daytona telemetry enable [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona template

ワークスペーステンプレートを管理する

```shell
daytona template [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytonaのヘルプ |

## daytona template create

ワークスペーステンプレートを作成する

```shell
daytona template create [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--builder` |  | ビルダーを指定する（現在はauto/devcontainer/none） |
| `--custom-image` |  | フラグ値として渡されたカスタムイメージでワークスペースを作成する；--custom-image-userフラグも設定する必要があります |
| `--custom-image-user` |  | フラグ値として渡されたカスタムイメージユーザーでワークスペースを作成する；--custom-imageフラグも設定する必要があります |
| `--devcontainer-path` |  | フラグ値として渡されたパスでdevcontainerビルダーを自動的に割り当てる |
| `--env` |  | 環境変数を指定する（例：--env 'KEY1=VALUE1' --env 'KEY2=VALUE2' ...） |
| `--git-provider-config` |  | GitプロバイダーのコンフィグレーションIDまたはエイリアスを指定する |
| `--label` |  | ラベルを指定する（例：--label 'label.key1=VALUE1' --label 'label.key2=VALUE2' ...） |
| `--manual` |  | Gitリポジトリを手動で入力する |
| `--name` |  | ワークスペーステンプレート名を指定する |
| `--help` |  | daytonaのヘルプ |

## daytona template delete

ワークスペーステンプレートを削除する

```shell
daytona template delete [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのワークスペーステンプレートを削除する |
| `--force` | `-f` | プリビルドを強制削除する |
| `--yes` | `-y` | プロンプトなしで削除を確認する |
| `--help` |  | daytonaのヘルプ |

## daytona template export

ワークスペーステンプレートをエクスポートする

```shell
daytona template export [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | すべてのワークスペーステンプレートをエクスポートする |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona template import

JSONオブジェクトからワークスペーステンプレートをインポートします

```shell
daytona template import [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--file` | `-f` | JSONファイルからワークスペーステンプレートをインポートします。標準入力から読み取るには'-'を使用してください。 |
| `--help` |  | daytonaのヘルプ |

## daytona template info

ワークスペーステンプレート情報を表示

```shell
daytona template info [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona template list

ワークスペーステンプレートを一覧表示します

```shell
daytona template list [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |

## daytona template set-default

ワークスペーステンプレート情報を設定

```shell
daytona template set-default [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytona のヘルプ |

## daytona template update

ワークスペーステンプレートを更新する

```shell
daytona template update [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona update

Daytona CLIを更新する

```shell
daytona update [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--version` | `-v` | 更新先のバージョン |
| `--help` |  | daytonaのヘルプ |

## daytona use

プロファイル [PROFILE_NAME] を使用する

```shell
daytona use [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | daytona のヘルプ |

## daytona version

バージョン番号を出力する

```shell
daytona version [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for daytona |

## daytona whoami

アクティブなユーザーに関する情報を表示します

```shell
daytona whoami [flags]
```

__フラグ__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | 出力形式。次のいずれかである必要があります (yaml, json) |
| `--help` |  | daytona のヘルプ |
