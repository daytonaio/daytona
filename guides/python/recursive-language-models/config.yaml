# Configuration for deeper-rlm

# Model configuration - using LiteLLM format
model:
  name: "openrouter/google/gemini-3-flash-preview"

# RLM configuration
rlm:
  max_sandboxes: 50
  max_iterations: 50
  global_timeout: 1800  # 30 minutes
  result_truncation_limit: 10000
