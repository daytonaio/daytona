name: daytona build
synopsis: Manage builds
inherited_options:
    - name: help
      default_value: "false"
      usage: help for daytona
see_also:
    - daytona - Daytona is a Dev Environment Manager
    - daytona build delete - Delete a build
    - daytona build info - Show build info
    - daytona build list - List all builds
    - daytona build logs - View logs for build
    - daytona build run - Run a build from a workspace config
