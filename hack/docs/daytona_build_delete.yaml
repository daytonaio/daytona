name: daytona build delete
synopsis: Delete a build
usage: daytona build delete [BUILD] [flags]
options:
    - name: all
      shorthand: a
      default_value: "false"
      usage: Delete ALL builds
    - name: prebuild-id
      usage: Delete ALL builds from prebuild
inherited_options:
    - name: help
      default_value: "false"
      usage: help for daytona
see_also:
    - daytona build - Manage builds
