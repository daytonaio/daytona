name: daytona template import
synopsis: Import a workspace template from a JSON object
usage: daytona template import [flags]
options:
    - name: file
      shorthand: f
      usage: |
        Import workspace template from a JSON file. Use '-' to read from stdin.
inherited_options:
    - name: help
      default_value: "false"
      usage: help for daytona
see_also:
    - daytona template - Manage workspace templates
