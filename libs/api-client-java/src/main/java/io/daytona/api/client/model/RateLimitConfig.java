/*
 * Daytona
 * Daytona AI platform API Docs
 *
 * The version of the OpenAPI document: 1.0
 * Contact: support@daytona.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.daytona.api.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.daytona.api.client.model.RateLimitEntry;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.daytona.api.client.JSON;

/**
 * RateLimitConfig
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.12.0")
public class RateLimitConfig {
  public static final String SERIALIZED_NAME_FAILED_AUTH = "failedAuth";
  @SerializedName(SERIALIZED_NAME_FAILED_AUTH)
  @javax.annotation.Nullable
  private RateLimitEntry failedAuth;

  public static final String SERIALIZED_NAME_AUTHENTICATED = "authenticated";
  @SerializedName(SERIALIZED_NAME_AUTHENTICATED)
  @javax.annotation.Nullable
  private RateLimitEntry authenticated;

  public static final String SERIALIZED_NAME_SANDBOX_CREATE = "sandboxCreate";
  @SerializedName(SERIALIZED_NAME_SANDBOX_CREATE)
  @javax.annotation.Nullable
  private RateLimitEntry sandboxCreate;

  public static final String SERIALIZED_NAME_SANDBOX_LIFECYCLE = "sandboxLifecycle";
  @SerializedName(SERIALIZED_NAME_SANDBOX_LIFECYCLE)
  @javax.annotation.Nullable
  private RateLimitEntry sandboxLifecycle;

  public RateLimitConfig() {
  }

  public RateLimitConfig failedAuth(@javax.annotation.Nullable RateLimitEntry failedAuth) {
    this.failedAuth = failedAuth;
    return this;
  }

  /**
   * Failed authentication rate limit
   * @return failedAuth
   */
  @javax.annotation.Nullable
  public RateLimitEntry getFailedAuth() {
    return failedAuth;
  }

  public void setFailedAuth(@javax.annotation.Nullable RateLimitEntry failedAuth) {
    this.failedAuth = failedAuth;
  }


  public RateLimitConfig authenticated(@javax.annotation.Nullable RateLimitEntry authenticated) {
    this.authenticated = authenticated;
    return this;
  }

  /**
   * Authenticated rate limit
   * @return authenticated
   */
  @javax.annotation.Nullable
  public RateLimitEntry getAuthenticated() {
    return authenticated;
  }

  public void setAuthenticated(@javax.annotation.Nullable RateLimitEntry authenticated) {
    this.authenticated = authenticated;
  }


  public RateLimitConfig sandboxCreate(@javax.annotation.Nullable RateLimitEntry sandboxCreate) {
    this.sandboxCreate = sandboxCreate;
    return this;
  }

  /**
   * Sandbox create rate limit
   * @return sandboxCreate
   */
  @javax.annotation.Nullable
  public RateLimitEntry getSandboxCreate() {
    return sandboxCreate;
  }

  public void setSandboxCreate(@javax.annotation.Nullable RateLimitEntry sandboxCreate) {
    this.sandboxCreate = sandboxCreate;
  }


  public RateLimitConfig sandboxLifecycle(@javax.annotation.Nullable RateLimitEntry sandboxLifecycle) {
    this.sandboxLifecycle = sandboxLifecycle;
    return this;
  }

  /**
   * Sandbox lifecycle rate limit
   * @return sandboxLifecycle
   */
  @javax.annotation.Nullable
  public RateLimitEntry getSandboxLifecycle() {
    return sandboxLifecycle;
  }

  public void setSandboxLifecycle(@javax.annotation.Nullable RateLimitEntry sandboxLifecycle) {
    this.sandboxLifecycle = sandboxLifecycle;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the RateLimitConfig instance itself
   */
  public RateLimitConfig putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    RateLimitConfig rateLimitConfig = (RateLimitConfig) o;
    return Objects.equals(this.failedAuth, rateLimitConfig.failedAuth) &&
        Objects.equals(this.authenticated, rateLimitConfig.authenticated) &&
        Objects.equals(this.sandboxCreate, rateLimitConfig.sandboxCreate) &&
        Objects.equals(this.sandboxLifecycle, rateLimitConfig.sandboxLifecycle)&&
        Objects.equals(this.additionalProperties, rateLimitConfig.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(failedAuth, authenticated, sandboxCreate, sandboxLifecycle, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class RateLimitConfig {\n");
    sb.append("    failedAuth: ").append(toIndentedString(failedAuth)).append("\n");
    sb.append("    authenticated: ").append(toIndentedString(authenticated)).append("\n");
    sb.append("    sandboxCreate: ").append(toIndentedString(sandboxCreate)).append("\n");
    sb.append("    sandboxLifecycle: ").append(toIndentedString(sandboxLifecycle)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("failedAuth");
    openapiFields.add("authenticated");
    openapiFields.add("sandboxCreate");
    openapiFields.add("sandboxLifecycle");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to RateLimitConfig
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!RateLimitConfig.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in RateLimitConfig is not found in the empty JSON string", RateLimitConfig.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `failedAuth`
      if (jsonObj.get("failedAuth") != null && !jsonObj.get("failedAuth").isJsonNull()) {
        RateLimitEntry.validateJsonElement(jsonObj.get("failedAuth"));
      }
      // validate the optional field `authenticated`
      if (jsonObj.get("authenticated") != null && !jsonObj.get("authenticated").isJsonNull()) {
        RateLimitEntry.validateJsonElement(jsonObj.get("authenticated"));
      }
      // validate the optional field `sandboxCreate`
      if (jsonObj.get("sandboxCreate") != null && !jsonObj.get("sandboxCreate").isJsonNull()) {
        RateLimitEntry.validateJsonElement(jsonObj.get("sandboxCreate"));
      }
      // validate the optional field `sandboxLifecycle`
      if (jsonObj.get("sandboxLifecycle") != null && !jsonObj.get("sandboxLifecycle").isJsonNull()) {
        RateLimitEntry.validateJsonElement(jsonObj.get("sandboxLifecycle"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!RateLimitConfig.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'RateLimitConfig' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<RateLimitConfig> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(RateLimitConfig.class));

       return (TypeAdapter<T>) new TypeAdapter<RateLimitConfig>() {
           @Override
           public void write(JsonWriter out, RateLimitConfig value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public RateLimitConfig read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             RateLimitConfig instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of RateLimitConfig given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of RateLimitConfig
   * @throws IOException if the JSON string is invalid with respect to RateLimitConfig
   */
  public static RateLimitConfig fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, RateLimitConfig.class);
  }

  /**
   * Convert an instance of RateLimitConfig to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

